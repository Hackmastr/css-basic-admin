namespace BasicAdmin;

internal enum TargetResult
{
    None,
    Multiple,
    Single
}

internal enum TargetFilter
{
    CounterTerrorist,
    Terrorist,
    Alive,
    Admin,
    Dead,
    Spec,
}
